{
  "name": "collage",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "version": "0.0.0",
  "scripts": {
    "dev:web": "pnpm --filter @collage/web dev",
    "dev:ext": "pnpm --filter @collage/extension dev",
    "dev:api": "pnpm --filter @collage/api dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  }
}
